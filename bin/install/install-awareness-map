#!/bin/bash

set -e

COMMITISH="544cb4f7f26ba301133905be5058c0cfa059a216"
REPO_URL="https://github.com/manueleigen/awareness-map"
INSTALL_DIR="/opt/awareness-map"

"${BASH_SOURCE%/*}/../internal/install-source-from-git" "$COMMITISH" "$REPO_URL" "$INSTALL_DIR"

# Build the project
cd "$INSTALL_DIR" || exit 1
corepack enable
corepack use npm@11
npm install
npm run build:all
corepack disable
